namespace NoireLib.UI;

/// <summary>Draws a custom row's control in the area the skin gave it.</summary>
/// <param name="area">Where the control goes.</param>
public delegate void SettingRowDraw(in SettingRowArea area);
